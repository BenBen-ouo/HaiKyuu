# HaiKyuu
2D online volleyball game

編譯檔案：javac -encoding UTF-8 -d build Main.java

Server 啟動指令：java -cp .\build Main server
雙方玩家啟動指令(看哪種可行)：
java -cp .\build Main join <以上Server的IP>
java Main join <以上Server的IP>